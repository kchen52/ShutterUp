package app.shutterup.share

/**
 * Copy for the share-a-card chrome (DESIGN.md §7). Kept next to the
 * exporter so Day and Completion cannot drift.
 */
object ShareCopy {
    const val BUTTON: String = "Share"
    const val FAILED: String = "The card didn't come together. Try again."
}
